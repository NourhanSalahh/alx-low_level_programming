#include "main.h"

/**
 * _abs - calculate absolute value of input
 * @num: input to be checked
 * Return: absolute value of input
 */

int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	return (-num);
}
